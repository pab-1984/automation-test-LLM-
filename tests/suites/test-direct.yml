suite: "Test Directo"
description: "Test de ejecución directa"
baseUrl: "http://localhost:3000"
mode: "direct"

tests:
  - name: "TC001 - Navegación básica"
    mode: "direct"
    steps:
      - action: "navigate"
        url: "${baseUrl}/"
        description: "Ir a la página principal"
      
      - action: "screenshot"
        filename: "pagina-directa"
    
    expectedResult: "La página carga en modo directo"
