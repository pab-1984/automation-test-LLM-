suite: "Test de Verificación - Dependencias Fijas"
description: "Test básico para verificar que el sistema funciona con dependencias fijas"
baseUrl: "https://example.com"
mode: "direct"

tests:
  - name: "TC001 - Verificar navegación básica"
    mode: "direct"
    steps:
      - action: "navigate"
        url: "${baseUrl}"
        description: "Navegar a example.com"

      - action: "screenshot"
        filename: "test-verificacion"
        description: "Capturar pantalla"

    expectedResult: "El navegador navega correctamente usando chrome-devtools-mcp local"
