# Test: prueba CLI
# Descripción: probar el agente de creacion de test

suite: "prueba CLI"
description: "probar el agente de creacion de test"
baseUrl: "http://localhost:3000"
timeout: 15000
mode: "llm"

tests:
  - name: "TC001 - Test básico"
    mode: "direct"
    steps:
      - action: "navigate"
        url: "${baseUrl}/"
        description: "Ir a la página principal"
      
      - action: "screenshot"
        filename: "prueba-cli-pagina-principal"
        description: "Captura de la página principal"
    
    expectedResult: "La página carga correctamente"
